const word = prompt('Ваше слово')
const wordLength = word.length //длина
wordLength ** 0.5 //корень кв
Math.pow(wordLength, 0.5) //корень кв
Math.sqrt(wordLength) //корень кв
const result = 1 //логику дописать
console.log(result)